# Copy to .env.local (git-ignored) and fill in the same Supabase project
# used by the web app (see the root repo's Vercel env vars).
# Expo only exposes vars prefixed with EXPO_PUBLIC_ to the app bundle.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
